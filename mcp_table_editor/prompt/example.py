from mcp_table_editor.editor.selector import Selector


def generate_example(selector: Selector) -> str:
    """Generate an example of the editing usage on dynamic.

    Parameters
    ----------
    selector : Selector
        Current selector object.
        The selector object is used to select the range of cells to be edited.

    Returns
    -------
    str
        Example of the editing usage.
    """
    ...
